# Translate Agent 使用说明

## 包含程序

| 文件 | 说明 |
| ---- | ---- |
| `tran.exe` | 命令行翻译（中英互转，大段文本） |
| `word.exe` | 命令行单词查询（本地词典 + AI 拓展） |
| `translate_gui.exe` | 桌面图形界面（翻译 + 单词 + 对话，无控制台窗口） |

首次使用前，请先配置 API（见下文）。三个程序共享同一份配置，配一次通用。

---

## 一、首次配置 API

打开 PowerShell 或 CMD，进入本目录，任选一种方式：

**方式 A：命令行配置（推荐）**
```
tran setup
```
按提示输入：
1. **API 地址**，例如：
   - DeepSeek：`https://api.deepseek.com`
   - OpenAI：`https://api.openai.com`
   - 通义千问：`https://dashscope.aliyuncs.com/compatible-mode`
   - Moonshot：`https://api.moonshot.cn`
   - 本地 Ollama：`http://localhost:11434`
2. **API Key**（如 `sk-...`）

程序会自动探测可用模型，输入序号选择即可。

**方式 B：图形界面配置**

双击 `translate_gui.exe` →「设置」→ 填写地址和 Key →「探测模型」→ 选模型 → 保存。

配置文件保存在：`C:\Users\<你的用户名>\.translate-agent\config.json`

---

## 二、翻译（tran.exe）

```
tran "hello world"            翻译（默认自动中英互转）
tran "你好世界"               自动检测方向：中文→英文
tran "hello" -o result.txt    译文写入文件
echo hello | tran             管道输入（CMD）
tran mode                     进入持续翻译模式（直接输入文本，Ctrl+C 退出）
tran list                     查看支持的语言
tran change                   切换目标语言（输入序号）
tran history                  查看翻译历史
tran setup                    重新配置 API
tran status                   查看当前配置
tran -h                       帮助
```

默认为「自动中英互转」模式：输入中文→翻译成英文，输入英文→翻译成中文。
用 `tran change` 可切换为固定目标语言（日语、韩语、法语等 15 种）。

**持续翻译模式**：运行 `tran mode` 后进入交互循环，直接输入文本即可翻译，无需重复输入 `tran`。输入 `exit` 或按 `Ctrl+C` 退出。

---

## 三、单词查询（word.exe）

```
word paper                    查询英文单词
word 你好                     查询中文对应的英文表达
word mode                     进入持续查词模式（直接输入单词，Ctrl+C 退出）
word fav <单词> [备注]        收藏/取消收藏单词
word favs                     查看单词本
word history                  查看查词历史
word list                     查看难度等级
word change                   切换难度（输入序号）
word setup                    重新配置 API
word status                   查看配置
word -h                       帮助
```

**查询流程**：
1. 先搜本地词典（5 万常用词，秒回，不消耗 API 额度）— 显示音标 + 释义
2. 再调 AI 生成拓展讲解（近义词、反义词、派生词、常用搭配、例句、助记）
3. 本地词典未收录的词，由 AI 完整输出

**难度等级**：初中 / 高中 / 高考 / 四级 / 六级 / 考研 / 专四 / 专八 / 雅思 / 托福 / GRE
难度影响例句复杂度和讲解深度。用 `word change` 切换。

**持续查词模式**：运行 `word mode` 后进入交互循环，直接输入单词即可查询。模式内也可使用 `fav <词>`、`favs`、`history`、`change`、`list` 等子命令。输入 `exit` 或按 `Ctrl+C` 退出。

---

## 四、图形界面（translate_gui.exe）

双击运行（不会弹出黑色控制台窗口）。

**翻译标签**：输入原文、选目标语言、点翻译。

**单词标签**：输入单词回车查询。右上角下拉框切换难度，点「★ 收藏」收藏单词。

**对话标签**：多轮对话，Enter 发送，Shift+Enter 换行。

**收藏标签**：查看单词本，支持删除、点击查询、导出 txt 文件。

**历史标签**：查看所有历史记录（翻译/单词/对话），可按类型筛选、点击重新查询、清空。

**设置标签**：配置 API 地址、Key、模型、翻译方向、单词难度。

---

## 五、数据文件

程序运行后会在 exe 同目录下创建 `data/` 文件夹：

| 文件 | 说明 |
| ---- | ---- |
| `data/history.json` | 历史记录（翻译 + 查词 + 对话，自动清理，维持在 30MB 以下） |
| `data/favorites.json` | 收藏的单词（无大小限制） |

三个程序共享这些数据文件。

---

## 六、环境要求

- Windows 10/11 x64
- 无需安装 Node.js 或其他软件
- 需要可用的 OpenAI 兼容 API（地址 + Key）
